name: Remove title number Y-shift patch

on:
  push:
    branches:
      - feature/mogai-applied
    paths:
      - .github/workflows/apply-title-number-yshift.yml
      - tools/apply_title_number_yshift_patch.py
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: feature/mogai-applied
          fetch-depth: 0

      - name: Sync branch
        run: |
          git fetch origin feature/mogai-applied
          git rebase origin/feature/mogai-applied

      - name: Remove patch
        run: python3 tools/apply_title_number_yshift_patch.py

      - name: Commit restored vrain.pl
        run: |
          if git diff --quiet -- vrain.pl; then
            echo "vrain.pl already restored"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add vrain.pl
          git commit -m "Remove chapter-number Y-shift patch"

          for attempt in 1 2 3 4 5; do
            git fetch origin feature/mogai-applied
            git rebase origin/feature/mogai-applied
            if git push origin HEAD:feature/mogai-applied; then
              exit 0
            fi
            sleep 5
          done
          echo "Failed to push restored vrain.pl after retries"
          exit 1
